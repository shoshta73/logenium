revision: 1
include/xheader/internal/xdg-shell-client-protocol.h:
  - if: sys.platform == 'linux'
    protocol_path: /usr/share/wayland-protocols/stable/xdg-shell/xdg-shell.xml
    command:
      - wayland-scanner
      - client-header
      - ${PROTOCOL_PATH}

  - if: sys.platform == 'win32'
    contents: |
      #ifndef LOGENIUM_XHEADER_INTERNAL_XHEADER_CLIENT_PROTOCOL_H
      #define LOGENIUM_XHEADER_INTERNAL_XHEADER_CLIENT_PROTOCOL_H

      #warning "This file empty on purpose on Windows"

      #endif

include/xheader/internal/xdg-decoration-unstable-v1-client-protocol.h:
  - if: sys.platform == 'linux'
    protocol_path: /usr/share/wayland-protocols/unstable/xdg-decoration/xdg-decoration-unstable-v1.xml
    command:
      - wayland-scanner
      - client-header
      - ${PROTOCOL_PATH}

  - if: sys.platform == 'win32'
    contents: |
      #ifndef LOGENIUM_XHEADER_INTERNAL_XDG_DECORATION_UNSTABLE_V1_PROTOCOL_H
      #define LOGENIUM_XHEADER_INTERNAL_XDG_DECORATION_UNSTABLE_V1_PROTOCOL_H

      #warning "This file empty on purpose on Windows"

      #endif

src/internal/xdg-shell-protocol.c:
  - if: sys.platform == 'linux'
    protocol_path: /usr/share/wayland-protocols/stable/xdg-shell/xdg-shell.xml
    command:
      - wayland-scanner
      - private-code
      - ${PROTOCOL_PATH}

  - if: sys.platform == 'win32'
    contents: |
      #include <xheader/internal/xdg-shell-client-protocol.h>

      #warning "This file empty on purpose on Windows"

src/internal/xdg-decoration-unstable-v1-protocol.c:
  - if: sys.platform == 'linux'
    protocol_path: /usr/share/wayland-protocols/unstable/xdg-decoration/xdg-decoration-unstable-v1.xml
    command:
      - wayland-scanner
      - private-code
      - ${PROTOCOL_PATH}

  - if: sys.platform == 'win32'
    contents: |
      #include <xheader/internal/xdg-decoration-unstable-v1-client-protocol.h>

      #warning "This file empty on purpose on Windows"
